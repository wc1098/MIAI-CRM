version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: miaicrm-postgres
    restart: no
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Anping520
      POSTGRES_DB: miaicrm
    ports:
      - "5432:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    networks:
      - miaicrm-network

  redis:
    image: redis:7-alpine
    container_name: miaicrm-redis
    restart: no
    command: redis-server --appendonly yes --requirepass Anping520
    ports:
      - "6379:6379"
    volumes:
      - ./data/redis:/data
    networks:
      - miaicrm-network

networks:
  miaicrm-network:
    driver: bridge
